# TODO

## Future Features

- [ ] Add lazy image loading for Blender synthetic datasets. The current lazy image loading implementation targets COLMAP datasets only; Blender datasets keep the existing eager image preparation path.
- [ ] Wire `full_eval.py`'s currently inactive `--addition_args`, `--densification_interval_list`, and `--rl` flags into the generated training commands.
